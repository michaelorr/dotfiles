Every file in this directory should be symlinked directly to ~/.*

i.e.
~/.skhdrc => ~/.dot/var/skhdrc
~/.tidy.conf => ~/.dot/var/.tidy.conf

copy a kitty icon into $XDG_CONFIG_HOME/kitty/kitty.app.png
