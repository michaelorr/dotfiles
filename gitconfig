[user]
    name = Michael Orr
    email = morr@rsglab.com
[color]
    branch = auto
    diff = auto
    ui = auto
    interactive = true
    status = auto
[color "branch"]
    current = yellow
    local = 241
    remote = green
[color "diff"]
    meta = yellow
    frag = magenta
    old = red
    new = green
[color "status"]
    added = green
    changed = yellow
    untracked = magenta
[core]
    excludesfile = ~/.dot/gitignore_global
    filemode = false
    autocrlf = false
    deltaBaseCacheLimit = 1G
    untrackedCache = true
[merge]
    conflictstyle = diff3
[github]
    user = morr
[alias]
    purgeBranches = "!f() { git branch --merged ${1-master} | grep -v \" ${1-master}$\" | xargs -n 1 git branch -d; }; f"
[init]
    templatedir = ~/.dot/git_templates
[url "git@git.rsglab.com:"]
    insteadOf = https://git.rsglab.com
